# راهنمای کامل NexoSelf

این سند تنها و تنها منبعِ راهنمای استفاده از ربات NexoSelf (سلف‌بات تلگرام) است. منشیِ هوش مصنوعیِ پشتیبانی باید جواب‌هاش را **فقط از همین سند** بردارد و هرگز چیزی از دانشِ عمومیِ خودش (مثلاً «به تنظیمات پروفایل تلگرام برو») اضافه نکند؛ NexoSelf هیچ ربطی به منوی تنظیماتِ رسمیِ تلگرام ندارد و کاملاً مستقل کار می‌کند.

---

### عضویتِ اجباری در کانال‌ها (پیش‌نیازِ استفاده از خودِ ربات)
قبل از هر کاری، ربات چک می‌کنه که کاربر عضوِ چندتا کانالِ مشخص باشه. اگه کاربر عضو نباشه یا از یکی از کانال‌ها لفت داده باشه، ربات به‌جای منو، یک پیام با لیستِ کانال‌ها نشون می‌ده — هر کانال یک دکمه‌ی «📢 عضویت در ...» داره که مستقیم می‌بره به همون کانال، و پایینش هم دکمه‌ی «✅ بررسی عضویت من» هست که بعد از جوین‌کردن باید بزنه تا ربات دوباره چک کنه و دسترسی بده. این چک هم روی `/start` و هم روی همه‌ی دکمه‌های اصلیِ منو (موجودی، هدیه روزانه، رفرال، خرید، ماموریت‌ها، ساخت/وصل‌کردنِ اکانت) اجرا می‌شه.

---

## ۱. فعال‌سازیِ اولیه (ساخت اکانت و وصل کردنِ سلف) — دقیق و مرحله‌به‌مرحله

اینجا رایج‌ترین سوالِ کاربرهاست: «چطور سلف رو فعال کنم؟». جواب دقیق این است:

۱. وارد چتِ ربات مدیریتیِ NexoSelf در تلگرام شو و `/start` بزن.
۲. اگه هنوز اکانت نساختی، پیام «اکانت نداری» رو با دکمه‌ی «🤖 ساخت اکانت با ربات» می‌بینی (همون دکمه‌ای که بعضی‌جاها «ورود سلف با ربات» یا «وصل کردن سلف» هم نامیده می‌شه — همه یک کارو می‌کنن).
۳. رو دکمه بزن؛ همه‌چیز مستقیم همین‌جا داخلِ چتِ ربات انجام می‌شه:
   - **مرحله ۱ از ۲ — شماره تلفن:** دکمه‌ی «📱 تایید شماره من» رو بزن تا شماره‌ی اکانتِ تلگرامی که می‌خوای سلف روش فعال بشه، خودکار برای ربات ارسال بشه (باید همون شماره‌ی خودِ اکانتت باشه، نه شخصِ دیگه).
   - ربات یک **کدِ تایید** به همون اکانتِ تلگرام (اپلیکیشن تلگرام رسمی، پیام Telegram) می‌فرسته.
   - **مرحله ۲ از ۲ — وارد کردنِ کد:** با کیبوردِ عددیِ داخلِ چت، کدِ دریافتی رو وارد کن و روی «تایید» بزن.
   - اگه اکانتت رمزِ دومرحله‌ای (2FA) داره، ربات ازت رمز رو هم می‌خواد؛ همونو تایپ و ارسال کن.
   - بعد از تاییدِ موفق، سِشِنِ سلف ساخته و ذخیره می‌شه و یک پیامِ خوش‌آمد + **یک روز سلف رایگان هدیه** می‌گیری.
۴. از این لحظه به بعد، سلف روی همون اکانتِ تلگرامی‌ات فعاله. برای روشن/خاموش کردنِ رفتارهای خودکارِ سلف (نه خودِ اتصال)، داخلِ هر چتِ تلگرام (مثلاً Saved Messages) تایپ کن:
   - `سلف روشن` → همه‌ی قابلیت‌های فعال‌شده کار می‌کنن
   - `سلف خاموش` → سلف موقتاً غیرفعال می‌شه (بدون قطع اتصال)

> ⚠️ نکته‌ی مهم: مراحلِ بالا **در خودِ ربات مدیریتیِ تلگرام** انجام می‌شه، نه در «تنظیمات» تلگرام. هیچ گزینه‌ای به اسمِ «فعال‌سازیِ سلف» داخلِ تنظیماتِ اکانتِ تلگرام وجود ندارد — این یک ربات و سیستمِ کاملاً جدا از تلگرام رسمی است. NexoSelf هیچ وب‌سایت یا پنلِ وبی نداره؛ همه‌چیز فقط از طریقِ همین ربات تلگرامی انجام می‌شه.

### اگه سلف قطع شد (وصل نبود)
اگه بعد از مدتی سلف قطع بشه (مثلاً به‌خاطرِ خروج از اکانت یا خطای سشن)، دفعه‌ی بعد که `/start` بزنی پیامِ «⚠️ سلف شما به اکانت وصل نیست» با دکمه‌ی «وصل کردن سلف» می‌بینی — همون دکمه رو بزن و دوباره مراحلِ بالا (شماره + کد) رو طی کن. اگه قبلاً شماره‌ات ثبت شده باشه، مستقیم کد براش فرستاده می‌شه و نیازی به فرستادنِ دوباره‌ی شماره نیست.

### اگه پیام «در انتظارِ تاییدِ ادمین» گرفتی
بعضی مالک‌ها تنظیم کرده‌ن که کاربرِ جدید باید اول تاییدِ دستیِ ادمین رو بگیره. اگه این پیام رو دیدی، فقط باید صبر کنی تا ادمین تاییدت کنه؛ کارِ دیگه‌ای لازم نیست.

---

## ۲. منوی اصلی ربات مدیریتی (دکمه‌های زیرِ پیامِ خوش‌آمد)

| دکمه | کاربرد |
|---|---|
| 💰 موجودی | نمایش موجودی الماس فعلی، کل الماس کسب‌شده، تعداد رفرال‌ها، قیمت هر الماس |
| 🎁 هدیه روزانه | دریافت الماس رایگان روزانه (هر ۲۴ ساعت یک‌بار) |
| 🔗 رفرال | دریافت لینک دعوت اختصاصی؛ با هر دعوت موفق الماس هدیه می‌گیری |
| 🛒 خرید | باز شدن منوی خرید اشتراک / خرید الماس |
| 🎯 ماموریت‌ها | نمایش ماموریت‌های قابل انجام برای کسب الماس بیشتر |
| ⚙️ مدیریت سلف | باز شدن پنل دکمه‌ای تنظیمات سلف (همه‌ی قابلیت‌ها از اینجا هم قابل روشن/خاموش کردنه، بدونِ نیاز به تایپِ دستور) |
| 📖 راهنما | نمایش راهنمای متنیِ همه‌ی دستورات |
| 🛟 پشتیبانی | ارتباط با پشتیبانیِ انسانی یا مکالمه با منشیِ هوش مصنوعی |

اگه رمزِ ادمینِ ربات هستی، به‌جای این دکمه‌ها یک کیبوردِ «مدیریت» و «مدیریت سلف» جدا می‌بینی که به پنلِ مدیریتِ کلِ ربات (کاربران، کانال‌های اجباری، پرداخت‌ها، تنظیمِ کارت، پیامِ همگانی و...) دسترسی می‌ده.

---

## ۳. سیستم الماس (توکن داخلی)

- الماس واحد پولِ داخلیِ ربات است و برای خرید اشتراک یا مصرف مستقیم استفاده می‌شه.
- **هدیه‌ی خوش‌آمد ثبت‌نام:** مقداری الماس رایگان بلافاصله بعد از ساختِ اکانت
- **یک روز سلفِ رایگان:** فقط برای کاربرِ تازه که هیچ اشتراکی نداره، خودکار فعال می‌شه
- **هدیه روزانه:** هر ۲۴ ساعت یک‌بار با زدنِ دکمه‌ی «🎁 هدیه روزانه» دریافت می‌شه؛ اگه زودتر از موعد بزنی، پیام می‌ده هنوز وقتش نرسیده
- **رفرال:** با دعوتِ هر کاربرِ جدید از طریقِ لینکِ اختصاصی‌ات (از دکمه‌ی «🔗 رفرال»)، الماسِ هدیه می‌گیری؛ لینک شبیهِ `https://t.me/BOT_USERNAME?start=ref_<آیدی_تو>` است
- **ماموریت‌ها:** انجامِ ماموریت‌های تعریف‌شده در بخشِ «🎯 ماموریت‌ها» هم الماسِ اضافه می‌ده

---

## ۴. مرحله‌به‌مرحله خرید اشتراک

### روش ۱: خرید اشتراک با الماس
۱. از منوی اصلی روی «🛒 خرید» بزن.
۲. گزینه‌ی «خرید اشتراک با الماس» رو انتخاب کن.
۳. یکی از پلن‌ها رو انتخاب کن: هفتگی / ماهانه / دوماهه.
۴. اگه موجودی الماست کافی باشه، اشتراک بلافاصله فعال می‌شه و تاریخ انقضا نشون داده می‌شه.
۵. اگه موجودی کافی نبود، پیام کمبود الماس می‌بینی با گزینه‌ی «خرید الماس» برای شارژ.

### روش ۲: خرید اشتراک با کارت‌به‌کارت
۱. از منوی «🛒 خرید»، گزینه‌ی «خرید اشتراک با کارت» رو بزن.
۲. یک پلن انتخاب کن.
۳. اگه کد تخفیف داری، «بله دارم» رو بزن و کد رو بفرست؛ وگرنه «ندارم» رو بزن.
۴. شماره‌کارت مقصد و مبلغ نهایی نشونت داده می‌شه.
۵. مبلغ رو کارت‌به‌کارت کن و عکس رسید رو در همون چت بفرست.
۶. رسید برای مالک/ادمین ارسال می‌شه؛ بعد از تایید، اشتراک به‌صورت خودکار فعال می‌شه و پیام تاییدیه دریافت می‌کنی.
۷. اگه رسید رد بشه، پیام رد شدن پرداخت رو دریافت می‌کنی.

### روش ۳: پرداخت ارزی (استارز تلگرام یا TON)
۱. از منوی «🛒 خرید»، گزینه‌ی «پرداخت ارزی» رو بزن — یک زیرمنو با دو گزینه باز می‌شه: «پرداخت با TON» و «پرداخت با استارز».

**۳‌الف) پرداخت با استارزِ تلگرام (Telegram Stars):**
۱. «پرداخت با استارز» رو بزن، یک پلن انتخاب کن (هفتگی/ماهانه/دوماهه)؛ تعدادِ استارزِ لازم برایِ همون پلن کنارِ اسمش نشون داده می‌شه.
۲. همون لحظه یک فاکتورِ پرداختِ استارز (مخصوصِ خودِ تلگرام) باز می‌شه؛ با موجودیِ استارزِ اکانتِ تلگرامی‌ات پرداخت کن.
۳. این روش کاملاً **خودکار** و آنیه — نیازی به فرستادنِ رسید یا تاییدِ دستیِ ادمین نیست؛ به‌محضِ موفقیتِ پرداخت، اشتراک بلافاصله فعال می‌شه و پیامِ تاییدیه با تاریخِ انقضا دریافت می‌کنی.
> اگه موجودیِ استارزِ کافی نداری، از همون فاکتورِ پرداخت (که خودِ تلگرام نشون می‌ده) گزینه‌ی خریدِ استارز رو می‌بینی.

**۳‌ب) پرداخت با TON:**
۱. «پرداخت با TON» رو بزن، یک پلن انتخاب کن؛ مبلغِ تقریبیِ TON کنارِ اسمِ پلن نشون داده می‌شه.
۲. بعد از انتخاب، یک **مبلغِ دقیقِ TON** (تا ۹ رقمِ اعشار) و **آدرسِ کیف‌پولِ مقصد** بهت نشون داده می‌شه.
۳. باید **دقیقاً همون مبلغ** رو از کیف‌پولِ خودت (Tonkeeper، کیف‌پولِ داخلِ تلگرام و…) به همون آدرس بفرستی — این مبلغ برای هر فاکتور یکتاست، برای همینه که سیستم بدونِ نیاز به مموی/کامنتِ روی تراکنش می‌تونه تشخیص بده این پرداخت مالِ کدوم فاکتوره.
۴. این فاکتور فقط تا **۳۰ دقیقه** معتبره؛ اگه توی این بازه پرداخت نشه، خودکار منقضی می‌شه و باید دوباره از اول اقدام کنی.
۵. تشخیصِ پرداخت کاملاً **خودکاره** — سیستم هر چند ثانیه یک‌بار تراکنش‌هایِ ورودی به کیف‌پول رو چک می‌کنه؛ به‌محضِ دیده‌شدنِ تراکنش با مبلغِ دقیق، اشتراک فعال می‌شه و پیامِ تاییدیه می‌گیری. نیازی به فرستادنِ رسید یا اسکرین‌شاتِ تراکنش نیست.
> ⚠️ اگه اشتباهاً مبلغِ متفاوتی بفرستی (حتی چند تومان کمتر/بیشتر)، سیستم نمی‌تونه تراکنش رو به فاکتورت وصل کنه؛ در این حالت باید به پشتیبانی پیام بدی.

### روش خرید مستقیم الماس (بدون خرید اشتراک)
۱. از منوی «🛒 خرید»، گزینه‌ی «خرید الماس» رو بزن.
۲. تعداد الماس موردنظر رو تایپ کن (حداقل خرید معمولاً ۱۰۰ الماس است؛ نرخ هر الماس ممکنه توسط مالک تغییر کنه).
۳. اگه کد تخفیف داری وارد کن.
۴. مبلغ نهایی و شماره‌کارت نشون داده می‌شه؛ پرداخت کن و رسید رو بفرست.
۵. بعد از تایید مالک/ادمین، الماس به موجودیت اضافه می‌شه.

> نکته: قیمت‌ها و تعداد الماسِ هر پلن ممکنه توسط مالک ربات تغییر کند؛ برای قیمت دقیق روز همیشه از داخل منوی «🛒 خرید» چک کن.

### چرا اشتراکم فعال نمی‌شه بعد از فرستادنِ رسید؟
تاییدِ رسید **دستی و توسطِ مالک/ادمین** انجام می‌شه، نه خودکار. یعنی باید صبر کنی تا ادمین رسید رو بررسی و تایید کنه. اگه مدتِ زیادی طول کشید، از «🛟 پشتیبانی» → «ارتباط با پشتیبانی» پیگیری کن.

---

## ۵. پنل «مدیریت سلف» (تنظیمات با دکمه)

از داخل ربات مدیریتی، دکمه‌ی «⚙️ مدیریت سلف» همه‌ی این دسته‌ها رو باز می‌کنه (هرکدوم دکمه‌های روشن/خاموش و اکشن‌های خودشو داره):

- **حالت متن**: نقل‌قول، زیرخط، اسپویلر، تدریجی، بولد، ایتالیک، خط‌خورده، تک‌فاصله، فینگلیش
- **ساعت**: ساعت روی نام، ساعت روی بیو، ساعت پرمیوم (با زیرمنوی فونت ساعت)
- **نگهبان چت**: ذخیره پیام حذف‌شده، ذخیره پیام ویرایش‌شده، ذخیره عکس تایمی (view once)
- **پینگ**: تست سرعت پاسخ سلف
- **لوگو**: ساخت لوگو از متن/عکس با ریپلای
- **قفل‌ها**: قفل یوزرنیم، قفل ریپلای، قفل گیف، قفل پیوی، قفل لینک، قفل عکس، قفل استیکر، قفل فوروارد، ضد حذف، قفل لاگین
- **اکشن**: تایپینگ، گیمینگ، ویس، ارسال ویدیو (نمایش خودکار وضعیت آنلاین) + زیرمنوی «اکشن به تفکیک گروه» (جزئیات در بخشِ «۵.۲»)
- **دوست و دشمن**: مدیریت لیست دوست و لیست دشمن
- **منشی**: پاسخ‌دهی خودکار هوش مصنوعی وقتی خودت آفلاینی
- **فیلترکلمات**: افزودن/حذف/لیست کلمات ممنوعه در پیوی
- **پاسخ خودکار**: پاسخ خودکار به پیام‌ها + پاسخ‌های کلیدواژه‌ای
- **عضویت اجباری پیوی**: اجبار عضویت در کانال قبل از پیام دادن به شما
- **دانلودر**: دانلود از پست‌های تلگرام/فایل‌ها
- **ریکت**: تنظیم واکنش خودکار روی پیام یک کاربر خاص
- **اسپم**: ارسال پیام تکراری با تاخیر قابل تنظیم
- **سایلنت**: نادیده‌گرفتن یک چت یا یک کاربر خاص
- **سکوت**: بی‌صدا کردن پیوی یک کاربر خاص (پیام‌هاش هردو طرف پاک می‌شه)
- **اطلاعات**: نمایش وضعیت کلی قابلیت‌های فعال
- **تگ**: منشن همه‌ی اعضای گروه
- **بلاک**: بلاک کردن و مشاهده‌ی لیست بلاک‌شده‌ها
- **حذف**: حذف پیام با ریپلای
- **هوش مصنوعی**: روشن/خاموش کردن پاسخگویی هوشمند + افزودن دانش اختصاصی
- **ترجمه**: ترجمه متن یا پیام ریپلای‌شده
- **تقلب**: نتیجه‌ی از پیش تعیین‌شده برای تاس/دارت/فوتبال/بسکتبال/کازینو
- **× ÷ (ماشین‌حساب)**: محاسبه عبارات ریاضی
- **تبچی**: مدیریت بنرهای تبلیغاتی خودکار در گروه‌ها
- **قیمت ارز**: نمایش قیمت دلار/تتر/یورو/پوند/بیت‌کوین
- **اسکرین**: ساخت استیکر از پیام یا پست کانال
- **ابزار بیشتر**: سین خودکار، ری‌اکشن خودکار، ذخیره مدیا، آب‌وهوا، پاکسازی بلاک، ترک همگانی گروه/کانال، تبدیل ویدیو به گیف، توقف سیو کانال، اطلاعات کاربر (آیدی)، دانلود پست تلگرام، دانلود اینستاگرام + زیرمنوی حذف همگانی پیوی‌ها
- **بازی Meowie**: اتوماسیون بازی گروهی Meowie (جزئیاتِ کامل در بخشِ «۵.۳ بازی میویی (Meowie)» زیر)

> راهنمای استفاده: هر دسته یک یا چند دکمه‌ی «روشن/خاموش» داره که با یک لمس وضعیتشو عوض می‌کنه؛ برای مواردی که نیاز به ورودی متنی دارن (مثلاً ماشین‌حساب یا ترجمه)، دکمه فقط یک پیامِ راهنما نشون می‌ده که باید چه دستوری تایپ کنی.

---

### ۵.۱ نحوه‌ی بازکردن پنل و رفتارِ کلیِ آن

پنلِ دکمه‌ای از دو مسیر باز می‌شه:
- دکمه‌ی «⚙️ مدیریت سلف» در منوی اصلیِ ربات مدیریتی.
- تایپِ `پنل` یا `panel` داخلِ هر چتِ تلگرام (مثلاً Saved Messages)؛ پیامِ خودت خودکار پاک می‌شه و همون‌جا پنلِ کامل ظاهر می‌شه — نیازی نیست حتماً برگردی به ربات مدیریتی.

**نکاتِ مهم درباره‌ی رفتارِ پنل:**
- **مالکیتِ پنل:** پنلی که برای تو باز شده فقط برای خودتِ قابلِ کلیک‌کردنه — حتی اگه در یک گروهِ مشترک باز شده باشه، اگه شخصِ دیگه‌ای روی دکمه‌هاش بزنه پیامِ «این پنل مخصوص کسی است که آن را باز کرده» می‌گیره و هیچ دستوری اجرا نمی‌شه.
- **بسته‌شدنِ خودکار بعد از بیکاری:** اگه ۳ دقیقه هیچ دکمه‌ای زده نشه، پنل خودکار بسته می‌شه و پیامِ «⏰ این پنل به‌خاطر ۳ دقیقه بیکار موندن بسته شد» جایگزینش می‌شه؛ برای بازکردنِ دوباره کافیه دوباره `پنل` رو تایپ کنی. هر بار که روی دکمه‌ای از همون پنل بزنی، این تایمرِ ۳‌دقیقه‌ای از نو شروع می‌شه.
- **بستنِ دستی:** داخلِ منوی اصلیِ پنل یک دکمه‌ی «بستن» هست که همون لحظه پنل رو می‌بنده.
- **صفحه‌بندی:** هم منوی اصلیِ دسته‌ها و هم لیستِ آیتم‌های داخلِ هر دسته (اگه تعدادشون زیاد باشه) صفحه‌بندی دارن؛ با دکمه‌های ورق‌زدن بین صفحه‌ها جابه‌جا می‌شی.
- **بازگشت:** داخلِ هر دسته یک دکمه‌ی «بازگشت» به منوی اصلیِ پنل هست.

---

### ۵.۲ «اکشن به تفکیک گروه»

این زیرمنو (داخلِ دسته‌ی «اکشن») به‌جای روشن/خاموش‌کردنِ سراسریِ اکشن‌ها، اجازه می‌ده هرکدوم از ۱۳ اکشنِ زیر رو **جدا برای هر گروه** روشن یا خاموش کنی:

تایپ، آپلود عکس، ضبط ویس، آپلود ویدیو، آپلود فایل، ضبط ویدیو، آپلود ویس، آپلود ویدیو نوت، ضبط ویدیو نوت، بازی، انتخاب مخاطب، پیدا کردن موقعیت، انتخاب استیکر.

روش کار: یک اکشن رو از لیست انتخاب می‌کنی، بعد لیستِ گروه‌هات با رنگِ سبز (فعال) یا قرمز (غیرفعال) نشون داده می‌شه؛ با زدنِ هر گروه، وضعیتِ همون اکشن فقط برای همون گروه عوض می‌شه، بدونِ اینکه روی بقیه‌ی گروه‌ها یا تنظیمِ سراسری تاثیر بذاره.

---

### ۵.۳ بازی میویی (Meowie)

این قابلیت، بازیِ گروهیِ Meowie (ربات‌های `@MeowieeeQBot`، `@MeowieQBot`، `@MeowieeQBot` — بسته به نسخه هرکدوم ممکنه توی گروه باشه) رو کاملاً خودکار می‌کنه: سلف به‌جای کاربر «میو» و «ماهی» می‌فرسته، وضعیتِ گربه رو با «پیشی» چک می‌کنه، وقتی موجودی برای ارتقا کافی شد خودش ارتقا می‌ده، و امتیازها رو برداشت می‌کنه — بدونِ اینکه کاربر لازم باشه پای گوشی بمونه.

**راه‌اندازیِ اولیه (مرحله‌به‌مرحله):**
۱. از پنلِ «⚙️ مدیریت سلف» وارد دسته‌ی «🐱 بازی Meowie» شو و دکمه‌ی «بازی میویی» رو بزن تا روشن بشه (یا مستقیم تایپ کن: `بازی میویی روشن`).
۲. داخلِ همون گروهی که ربات Meowie توش فعاله، یک‌بار **دستی** بنویس `میو`. همین یک پیام کافیه تا آن گروه به‌عنوان «گروه بازی» ثبت بشه؛ از این لحظه به بعد ارسالِ «میو»/«ماهی» کاملاً خودکاره و نیازی به تکرارِ دستی نیست.
۳. برای فعال شدنِ خودکارسازیِ «پیشی» (وضعیتِ شکمِ گربه + ارتقای سطح + برداشتِ امتیاز)، کافیه یک‌بار **دستی** داخل همون گروه `پیشی` رو بفرستی. تا وقتی این یک‌بار انجام نشه، بخشِ پیشی شروع نمی‌شه — چون سلف تا اون لحظه نه نرخِ پرشدنِ ظرفیت رو می‌دونه نه سقفِ فعلیِ گربه رو، پس چیزی برای زمان‌بندی نداره.
۴. برای عوض کردنِ گروهِ بازی (مثلاً انتقال به گروهِ دیگه)، تایپ کن `ریست گروه بازی میویی` (یا `حذف گروه میویی`) و بعد دوباره یک‌بار دستی `میو` رو در گروهِ جدید بفرست.

**بعد از راه‌اندازی، این‌ها کاملاً خودکار انجام می‌شن:**
- ارسالِ «میو» و «ماهی» هروقت کول‌داونِ هرکدوم تموم بشه (زمانِ دقیق از روی پاسخِ خودِ ربات بازی خونده می‌شه، نه یک تایمرِ ثابت).
- وقتی گربه سیر باشه، ماهیِ اضافه یا فروخته می‌شه یا توی یخچال گذاشته می‌شه؛ اگه ارزشِ ماهی به‌اندازه‌ی کافی بالا باشه، حتی با گربه‌ی سیر هم به‌جای یخچال، فروخته می‌شه (برای جلوگیری از هدررفتنِ ماهیِ ارزشمند).
- ارسالِ «پیشی» هر بار که ظرفیتِ گربه پر می‌شه (بعد از اولین ارسالِ دستی طبقِ مرحله‌ی ۳).
- کلیکِ خودکارِ دکمه‌ی «برداشت میو پوینت ها» روی پیامِ پیشی.
- ارسالِ «پیشی» + کلیکِ خودکارِ «ارتقا سطح» وقتی موجودی برای ارتقا کافی باشه.
- ارسالِ دوره‌ای «میوهام» (پیش‌فرض هر ۶ ساعت) برای چکِ خودکارِ موجودی/ارتقا.
- کلیکِ خودکار روی دکمه‌ی «نجات پیشی خیابونی» هروقت این پیام ظاهر بشه.

**روشن/خاموش کردنِ جدا‌جداِ هرکدوم (بدونِ خاموش کردنِ کلِ بازی):**
از پنل، داخلِ «🐱 بازی Meowie» → «⚙️ تنظیمات» برو تا این هفت‌تا رو جدا از هم روشن/خاموش کنی: میو خودکار، ماهی خودکار، فروش ماهی خودکار، پیشی خودکار، برداشت خودکار امتیاز، ارتقا خودکار پیشی، چک خودکار موجودی، نجات خودکار پیشی خیابونی. همه‌شون پیش‌فرض روشن‌ان؛ خاموش‌کردنِ هرکدوم فقط همون بخش رو غیرفعال می‌کنه، بقیه دست‌نخورده کار می‌کنن.

> ⚠️ نکته: این قابلیت فقط دستورهای متنیِ سادهٔ همون بازی (میو/ماهی/پیشی/میوهام) رو طبق تایمری که خودِ ربات Meowie اعلام می‌کنه ارسال می‌کنه و دکمه‌های خودِ همون پیام‌ها رو می‌زنه؛ چیزی فراتر از رفتارِ عادیِ یک بازیکن انجام نمی‌ده.

---

## ۶. دستورات متنی (تایپ در چتِ تلگرام بعد از فعال‌سازیِ سلف)

> برای اینکه این دستورات کار کنن، سلف باید قبلاً طبقِ «بخش ۱» فعال شده باشه و `سلف روشن` هم زده باشی.

### اصلی
- `سلف روشن` / `سلف خاموش`
- `وضعیت` — نمایش وضعیت همه‌ی قابلیت‌ها
- `راهنما` — نمایش کامل دستورات
- `پنل` یا `panel` — **راهِ دومِ بازکردنِ پنلِ دکمه‌ایِ مدیریتِ سلف**؛ کافیه همینو مستقیم توی هر چتِ تلگرام (مثلاً Saved Messages) تایپ کنی، پیامت خودکار پاک می‌شه و همون‌جا پنلِ کاملِ دکمه‌ایِ سلف (همون چیزی که با «⚙️ مدیریت سلف» توی ربات باز می‌شه) ظاهر می‌شه — نیازی نیست حتماً برگردی به ربات مدیریتی

### دوست و دشمن
- `تنظیم دشمن` (ریپلای) / `حذف دشمن` (ریپلای یا آیدی)
- `نمایش لیست دشمن` / `پاک کردن لیست دشمن`
- `تنظیم دوست` (ریپلای) / `حذف دوست` (ریپلای یا آیدی)
- `نمایش لیست دوست` / `پاک کردن لیست دوست`

### منشی
- `منشی روشن` / `منشی خاموش`
- `پیام منشی [متن]`
- هر کاربر هر ۲۴ ساعت یک‌بار پاسخ می‌گیرد

### امنیت
- `ضد حذف روشن/خاموش`، `ضد لینک روشن/خاموش`، `قفل پیوی روشن/خاموش`
- `پاسخ دشمن روشن/خاموش`
- `سکوت [آیدی یا یوزرنیم]` (ریپلای یا آیدی) / `لغو سکوت` / `لیست سکوت`

### قفل‌ها
- `قفل یوزرنیم روشن/خاموش` (با منشن @username)
- `قفل ریپلای`، `قفل گیف`، `قفل عکس`، `قفل استیکر`، `قفل فوروارد`، `قفل لاگین` (روشن/خاموش)

### فیلتر کلمات
- `فیلتر کلمه [کلمه]` / `حذف فیلتر کلمه [کلمه]`
- `لیست فیلتر کلمات` / `فیلترکلمات روشن/خاموش`

### هوش مصنوعی (دستیارِ خودِ سلف — نه پشتیبانی)
- `دیپ سیک روشن/خاموش`
- `هوش مصنوعی پاسخ همه روشن/خاموش`
- `آموزش هوش مصنوعی [متن]` — افزودن دانش اختصاصی
- `نمایش دانش هوش مصنوعی` / `پاک کردن دانش هوش مصنوعی`
- هر کاربری که با شما پیوی می‌زنه، حداکثر روزی ۱۰ پیام از این دستیار جواب می‌گیره؛ بعد از سقف، یک پیامِ اطلاع‌رسانی می‌گیره و باید فردا دوباره پیام بده.

### پاسخ خودکار
- `پاسخ خودکار روشن/خاموش`
- `متن پاسخ خودکار [متن]`
- `پاسخ کلیدی [کلمه] [پاسخ]` / `حذف پاسخ کلیدی [کلمه]`
- `لیست پاسخ کلیدی` / `پاک کردن پاسخ کلیدی`

### جوین اجباری
- `افزودن کانال [آیدی یا @یوزرنیم] [لینک]`
- `حذف کانال [آیدی یا @یوزرنیم]`
- `لیست کانال‌های اجباری` / `پاک کردن کانال‌های اجباری`
- `پیام جوین [متن]`
- `جوین اجباری روشن/خاموش`

### اتوماسیون
- `سین خودکار روشن/خاموش`
- `ری‌اکشن روشن/خاموش` / `ری‌اکشن [ایموجی]`
- `تنظیم ری‌اکت [ایموجی]` (ریپلای) / `حذف ری‌اکت` (ریپلای)
- `ذخیره مدیا روشن/خاموش`
- `ذخیره عکس تایمی روشن/خاموش`
- `ذخیره پیام حذف‌شده روشن/خاموش`
- `ذخیره پیام ویرایش‌شده روشن/خاموش`
- `ساعت نام روشن/خاموش`، `ساعت بیو روشن/خاموش`، `ساعت پرمیوم روشن/خاموش`

### اکشن خودکار
- `اکشن` — نمایش وضعیت
- `تایپینگ روشن/خاموش`، `گیمینگ روشن/خاموش`، `ویس روشن/خاموش`، `ارسال ویدیو روشن/خاموش`

### مدیریت
- `تگ [متن]` (فقط گروه) / `لغو تگ`
- `حذف` (ریپلای)
- `ایدی [آیدی/یوزرنیم]` (یا ریپلای) — کارت اطلاعات کاربر
- `لیست بلاک` / `پاکسازی لیست بلاک`

### ابزار
- `محاسبه [عبارت]` — مثال: `محاسبه 5*(2+3)`
- `ترجمه [متن]` یا `ترجمه متن` (ریپلای)
- `.هوا [شهر]`
- `ارز` یا `ارز دلار` / `ارز تتر` / `ارز یورو` / `ارز پوند` / `ارز بیت کوین`
- `لوگو` (ریپلای روی متن/عکس)
- `پینگ`
- `.اسکرین` (ریپلای) یا `.اسکرین [لینک پست کانال]`
- `تبدیل به گیف` (ریپلای روی ویدیو)
- `دانلود [لینک پست تلگرام]`
- `اینستا [لینک پست/ریل اینستاگرام]`

### اسپم
- `اسپم [تعداد] [متن]` — مثال: `اسپم 100 سلام`
- `توقف اسپم`
- `تاخیر اسپم [ثانیه]`

### سایلنت
- `سایلنت چت روشن/خاموش`
- `سایلنت کاربر [آیدی]` / `لغو سایلنت کاربر [آیدی]`

### ترک/حذف همگانی
- `ترک همگانی گروه` / `ترک همگانی کانال`
- `حذف دوطرفه پیوی ها` / `حذف یکطرفه پیوی ها`

### پیام
- `ذخیره [1-10]` (ریپلای) / `ارسال ذخیره [1-10]`
- `حذف بعد [ثانیه]`
- `ارسال زمان‌بندی [YYYY-MM-DD HH:MM] متن`

### سیو مدیا
- `سیو کانال [لینک پست]`
- `سیو کانال [@کانال] [تعداد]`
- `توقف سیو`

### تبچی (بنر)
- `تبچی روشن/خاموش`
- `تنظیم بنر [1-10]` (ریپلای روی متن/عکس/ویدیو)
- `تنظیم بنر [1-10] در همه گروه‌ها`
- `فعال کردن بنر [1-10]` / `غیرفعال کردن بنر [1-10]`
- `لیست بنرها` / `پاکسازی لیست بنر`

### قالب‌بندی متن (فارسی/انگلیسی)
- `بولد [متن]`، `ایتالیک [متن]`، `مونو [متن]`، `اسپویلر [متن]`، `کوت [متن]`، `خط‌خورده [متن]`، `زیرخط [متن]`

### فونت
- `.فونت [0-8]` یا `.فونت [متن] [0-8]`
- `.لیست فونت`
- `.فونت متن روشن/خاموش` — ادیت خودکار هر پیام با فونت انتخابی
- `.فونت ساعت [0-9]` / `.لیست فونت ساعت`

### بازی‌ها
- `.تاس [1-6]` یا `.roll [1-6]`
- `.دارت [1-6]`
- `.فوتبال [1-5]`
- `.بسکتبال [1-5]`
- `.کازینو [متن]`
- `بازی میویی روشن` / `بازی میویی خاموش` — روشن/خاموش کردنِ کلِ اتوماسیونِ بازی Meowie
- `میو` (دستیِ داخلِ گروه، فقط یک‌بار لازم) — بایند کردنِ گروهِ بازی Meowie برای اتوماسیون (ربات‌های پشتیبانی‌شده: `@MeowieeeQBot`, `@MeowieQBot`, `@MeowieeQBot`)؛ بعدش خودِ «میو» و «ماهی» خودکار می‌شن
- `پیشی` (دستیِ داخلِ گروهِ بایند‌شده، فقط یک‌بار لازم) — شروعِ خودکارسازیِ چکِ وضعیتِ گربه/ارتقا/برداشتِ امتیاز
- `ریست گروه بازی میویی` یا `حذف گروه میویی` — حذفِ گروهِ ثبت‌شده برای بایندِ دوباره روی گروهِ جدید
- جزئیاتِ کامل (مرحله‌به‌مرحله + تاگل‌های ریز) در بخشِ «۵.۳ بازی میویی (Meowie)»

### نکات مهم
- در گروه‌ها فقط وقتی تگ بشید سلف پاسخ می‌ده.
- پاسخ به دوستان هر ۱ ساعت یک‌بار محدود می‌شه.
- بیشترِ دستورات فارسی‌ان و باید دقیقاً همون‌طور که نوشته شده (بدون غلط املایی) تایپ بشن.
- دستوراتی که با نقطه شروع می‌شن (مثل `.فونت`، `.اسکرین`، `.تاس`) حتماً باید همون نقطه رو داشته باشن.

---

## ۷. پشتیبانی

از منوی اصلی روی «🛟 پشتیبانی» بزن:
- **ارتباط با پشتیبانی**: پیامت مستقیم برای مالک/ادمین ارسال می‌شه و بعد جواب می‌گیری.
- **مکالمه با هوش مصنوعی**: سوالاتِ مربوط به نحوه‌ی استفاده از NexoSelf رو از منشی هوش مصنوعی بپرس.

این دکمه برای همه در دسترسه — چه کاربرِ تازه که هنوز اکانت نساخته، چه کاربری که سلفش قطع شده، چه کاربرِ فعال.

اگه سوالت فنی/داخلی بود (مثل ساختار کد یا دیتابیس) یا هوش مصنوعی جواب قانع‌کننده نداشت، از گزینه‌ی «ارتباط با پشتیبانی» استفاده کن تا مستقیم با مالک/ادمین صحبت کنی.

---

## ۸. سوالاتِ متداول (FAQ) — برای تطبیقِ سریع‌ترِ منشیِ هوش مصنوعی

**س: چطور سلف رو فعال/روشن کنم؟**
ج: اول باید از منوی اصلی دکمه‌ی «ساخت اکانت با ربات» (یا «ورود سلف با ربات» / «وصل کردن سلف») رو بزنی و شماره‌ی اکانتت + کدِ تاییدی که برات میاد رو وارد کنی (بخش ۱ همین راهنما). بعد از وصل شدن، برای فعال کردنِ رفتارهای خودکار در چت تایپ کن: `سلف روشن`

**س: کد تایید نیومد، چیکار کنم؟**
ج: چند دقیقه صبر کن؛ اگه بازم نیومد، دوباره روی «وصل کردن سلف» بزن تا کد دوباره ارسال بشه. اگه بازم مشکل داشت، از «🛟 پشتیبانی» پیگیری کن.

**س: خرید با استارزِ تلگرام یا TON چه فرقی با بقیه‌ی روش‌ها داره؟**
ج: هردوتاشون کاملاً خودکارن — نه رسید لازم دارن نه تاییدِ دستیِ ادمین. از منوی «🛒 خرید» → «پرداخت ارزی»، بینِ استارز یا TON انتخاب کن، پلن رو انتخاب کن و پرداخت کن؛ همون لحظه (یا برای TON، چند ثانیه بعد از تاییدِ بلاک‌چین) اشتراک فعال می‌شه.

**س: توی پرداختِ TON چرا باید دقیقاً همون عددِ اعشاری رو بفرستم؟**
ج: چون هیچ مموی/کامنتی روی تراکنشِ TON اجباری نیست و خیلی از کاربرا یادشون می‌ره پرش کنن، سیستم به‌جاش از خودِ مبلغِ دقیق (که برایِ هر فاکتور یکتاست) برای تشخیصِ اینکه این پرداخت مالِ کدوم کاربره استفاده می‌کنه. اگه مبلغ رو گرد کنی یا دستکاری کنی، سیستم نمی‌تونه پرداخت رو پیدا کنه.

**س: چطور اشتراک بخرم؟**
ج: از منوی «🛒 خرید»، یا با الماس یا با کارت‌به‌کارت یا با پرداختِ ارزی — استارز یا TON (بخش ۴ همین راهنما).

**س: موجودی الماسم رو از کجا ببینم؟**
ج: دکمه‌ی «💰 موجودی» در منوی اصلی.

**س: چطور الماسِ رایگان بگیرم؟**
ج: هدیه‌ی روزانه (هر ۲۴ ساعت)، دعوت دوستان با لینک رفرال، و انجام ماموریت‌ها.

**س: سلف من قطع شده، دوباره چطور وصلش کنم؟**
ج: `/start` بزن، پیامِ «سلف وصل نیست» با دکمه‌ی «وصل کردن سلف» میاد؛ بزنش و کدِ تایید رو وارد کن.

**س: چطور با پشتیبانیِ انسانی صحبت کنم؟**
ج: از منوی اصلی «🛟 پشتیبانی» → «ارتباط با پشتیبانی».

**س: چطور سلف رو خاموش کنم بدون اینکه اتصالش قطع بشه؟**
ج: کافیه تایپ کنی `سلف خاموش`. این فقط رفتارهای خودکار رو متوقف می‌کنه؛ سشن و اتصال دست‌نخورده می‌مونه و هر وقت خواستی با `سلف روشن` دوباره فعالش کن.

**س: چرا وقتی توی گروه بهم پیام می‌دن سلف جواب نمی‌ده؟**
ج: طبیعیه — سلف توی گروه‌ها فقط وقتی که تگ/منشن بشی جواب می‌ده، نه به هر پیامی.

**س: چطور جلوی اسپم/مزاحمتِ یک نفر رو بگیرم؟**
ج: یا `تنظیم دشمن` (با ریپلای روی پیامش) بزن، یا برای پیوی مشخص `سکوت [آیدی]` رو تایپ کن تا پیام‌هاش دیده نشه و هردو طرف پاک بشه.

**س: چطور بفهمم چه قابلیت‌هایی الان فعالن؟**
ج: تایپ کن `وضعیت` تا لیستِ کاملِ قابلیت‌های روشن/خاموش رو ببینی، یا از پنلِ «⚙️ مدیریت سلف» → «اطلاعات».

**س: منشی هوش مصنوعیِ خودم (که وقتی آفلاینم جواب می‌ده) با هوش مصنوعیِ پشتیبانی یکی‌ان؟**
ج: نه، کاملاً جدان. «منشی» قابلیتیه که خودِ سلف وقتی تو آفلاینی به پیام‌های دیگران جواب می‌ده (با `منشی روشن` فعال می‌شه). «هوش مصنوعیِ پشتیبانی» همینه که الان داره به تو جواب می‌ده و فقط درباره‌ی نحوه‌ی کارکردِ NexoSelf اطلاعات می‌ده.

**س: بازی میویی (Meowie) چطور فعال می‌شه؟**
ج: اول از پنل «⚙️ مدیریت سلف» → «🐱 بازی Meowie» دکمه‌ی «بازی میویی» رو روشن کن (یا تایپ کن `بازی میویی روشن`). بعد داخلِ گروهی که ربات Meowie توش فعاله، فقط یک‌بار دستی بنویس `میو` تا گروه بایند بشه. از اون به بعد میو/ماهی خودکاره. برای اینکه بخشِ «پیشی» (وضعیتِ گربه، ارتقا، برداشتِ امتیاز) هم خودکار بشه، یک‌بار دیگه دستی `پیشی` رو همون‌جا بفرست. جزئیاتِ کامل در بخشِ «۵.۳» همین راهنما.

**س: چطور گروهِ بازی میویی رو عوض کنم؟**
ج: تایپ کن `ریست گروه بازی میویی` (یا `حذف گروه میویی`)، بعد داخلِ گروهِ جدید یک‌بار دیگه دستی `میو` رو بفرست تا همون گروهِ جدید بایند بشه.

**س: می‌خوام فقط بخشِ ارتقا/برداشتِ امتیازِ بازی میویی خاموش باشه ولی میو/ماهیِ خودکار ادامه داشته باشه، ممکنه؟**
ج: بله. از پنل، داخلِ «🐱 بازی Meowie» → «⚙️ تنظیمات»، هرکدوم از قابلیت‌های ریز (میو خودکار، ماهی خودکار، فروش ماهی خودکار، پیشی خودکار، برداشت خودکار امتیاز، ارتقا خودکار پیشی، چک خودکار موجودی، نجات خودکار پیشی خیابونی) رو جدا از بقیه روشن/خاموش کن؛ لازم نیست کلِ «بازی میویی» رو خاموش کنی.

**س: چرا وقتی دکمه‌های پنل رو توی گروه می‌زنم، پیامِ «این پنل مخصوص کسی است که آن را باز کرده» می‌گیرم؟**
ج: پنل فقط برای کسی که با تایپِ `پنل` یا زدنِ «⚙️ مدیریت سلف» بازش کرده کار می‌کنه؛ حتی توی گروهِ مشترک، بقیه نمی‌تونن دکمه‌هاش رو بزنن. هرکس برای پنلِ خودش باید `پنل` رو خودش تایپ کنه.

**س: پنل رو باز گذاشتم و برگشتم، ولی دیگه دکمه‌هاش کار نمی‌کنه، چرا؟**
ج: هر پنل اگه ۳ دقیقه بدونِ کلیک بمونه خودکار بسته می‌شه (پیامِ «⏰ این پنل به‌خاطر ۳ دقیقه بیکار موندن بسته شد» جایگزینش می‌شه). کافیه دوباره `پنل` رو تایپ کنی تا از نو باز بشه.

**س: می‌خوام «تایپینگ» فقط توی یک گروهِ خاص روشن باشه، نه همه‌جا؛ ممکنه؟**
ج: بله، از پنل، دسته‌ی «اکشن» → «اکشن به تفکیک گروه» → همون اکشن (مثلاً تایپ) رو انتخاب کن، بعد فقط روی همون گروهِ موردنظر بزن تا سبز (فعال) بشه؛ بقیه‌ی گروه‌ها و تنظیمِ سراسری دست‌نخورده می‌مونن.

**س: آیا NexoSelf اکانتم رو هک یا بلاک می‌کنه؟**
ج: NexoSelf فقط با اجازه‌ی خودِ کاربر و از طریقِ همون فرآیندِ فعال‌سازیِ رسمی (شماره + کدِ تایید) به اکانت وصل می‌شه. استفاده‌ی بیش‌ازحد از برخی قابلیت‌ها (مثلِ اسپمِ سنگین) ممکنه توسطِ خودِ تلگرام محدودیت ایجاد کنه؛ برای جزئیاتِ بیشتر از «🛟 پشتیبانی» بپرس.

---

## ۹. عیب‌یابیِ رایج (Troubleshooting)

| مشکل | راه‌حل |
|---|---|
| دستور تایپ‌شده جواب نمی‌ده | مطمئن شو `سلف روشن` هست، دستور رو دقیقاً بدونِ غلطِ املایی و با فاصله‌گذاریِ درست تایپ کردی، و اگه دستور با نقطه شروع می‌شه (مثلِ `.فونت`) نقطه رو ننداختی |
| کدِ تاییدِ ثبت‌نام نمی‌رسه | چند دقیقه صبر کن؛ اگه نیومد دوباره روی «وصل کردن سلف» بزن تا کد دوباره فرستاده بشه |
| بعد از فرستادنِ رسیدِ خرید هنوز اشتراک فعال نشده | تاییدِ رسید دستیه و توسطِ ادمین انجام می‌شه؛ باید صبر کنی. اگه طول کشید از «ارتباط با پشتیبانی» پیگیری کن |
| موجودیِ الماس درست نشون داده نمی‌شه | دکمه‌ی «💰 موجودی» رو دوباره بزن تا تازه بشه؛ اگه بازم اشتباه بود پیگیری کن |
| سلف بعد از مدتی خودش قطع می‌شه | معمولاً به‌خاطرِ خروج از اکانت یا نامعتبر شدنِ سشنه؛ کافیه دوباره از «وصل کردن سلف» طیِ مراحل کنی |
| هدیه‌ی روزانه فعال نمی‌شه | یعنی هنوز ۲۴ ساعت از آخرین دریافت نگذشته؛ باید صبر کنی |
| قفل/دستوری که روشن کردم اثر نداره | چک کن که واقعاً روی «روشن» تنظیم شده (با تایپِ `وضعیت` یا از پنلِ «مدیریت سلف» → «اطلاعات»)؛ بعضی قفل‌ها فقط توی پیوی یا فقط توی گروه اثر دارن |
| هوش مصنوعیِ پشتیبانی جواب نامرتبط یا اشتباه می‌ده | با «ارتباط با پشتیبانی» مستقیم با ادمین صحبت کن |

---

## ۱۰. واژه‌نامه (Glossary)

- **سلف (Selfbot):** خودِ اکانتِ تلگرامیِ کاربر که با اتصال به NexoSelf، قابلیت‌های خودکار روش فعال می‌شه.
- **الماس:** واحدِ پولِ داخلیِ ربات برای خرید اشتراک.
- **اشتراک:** بازه‌ی زمانی که سلف مجاز به کار کردنه (هفتگی/ماهانه/دوماهه یا یک‌روزِ رایگان).
- **پنلِ مدیریتِ سلف:** بخشِ دکمه‌ایِ داخلِ ربات که همه‌ی تنظیمات رو بدونِ تایپِ دستور در اختیار می‌ذاره.
- **منشی:** قابلیتی که وقتی خودِ کاربر آفلاینه، به‌جاش با هوش مصنوعی به پیام‌ها جواب می‌ده.
- **قفل:** مجموعه‌ای از محدودیت‌های خودکار (مثلِ قفلِ لینک، قفلِ عکس) که سلف روی پیوی/گروه اعمال می‌کنه.
- **کارت‌به‌کارت:** روشِ پرداختِ دستی با واریزِ مستقیم و فرستادنِ رسید برای تاییدِ ادمین.
- **رفرال:** سیستمِ دعوتِ دوستان با لینکِ اختصاصی برای کسبِ الماسِ پاداش.
- **بازی میویی (Meowie):** قابلیتی که ارسالِ خودکارِ «میو»/«ماهی»/«پیشی»/«میوهام» و کلیکِ خودکارِ دکمه‌های بازیِ گروهیِ Meowie رو انجام می‌ده تا کاربر لازم نباشه دستی پیگیرِ کول‌داون‌ها باشه (بخشِ ۵.۳).
